exports.handler=async(event)=>{const headers={'Access-Control-Allow-Origin':'*','Content-Type':'application/json'};if(event.httpMethod==='OPTIONS')return{statusCode:200,headers,body:''};try{const{action,email,plan,amount,description,factuurNr}=JSON.parse(event.body||'{}');const sk=process.env.STRIPE_SECRET_KEY;if(!sk)return{statusCode:500,headers,body:JSON.stringify({error:'STRIPE_SECRET_KEY niet ingesteld'})};if(action==='create_checkout'){const amounts={starter:900,pro:1900};const amt=amounts[plan]||900;const name=plan==='pro'?'SlimBoekhoud Pro':'SlimBoekhoud Starter';const params=new URLSearchParams({'mode':'subscription','payment_method_types[]':'card','payment_method_types[]':'ideal','customer_email':email||'','line_items[0][price_data][currency]':'eur','line_items[0][price_data][product_data][name]':name,'line_items[0][price_data][unit_amount]':amt,'line_items[0][price_data][recurring][interval]':'month','line_items[0][quantity]':1,'success_url':'https://slimboekhoud.com?betaald=true','cancel_url':'https://slimboekhoud.com?betaald=geannuleerd','subscription_data[trial_period_days]':30});const res=await fetch('https://api.stripe.com/v1/checkout/sessions',{method:'POST',headers:{'Authorization':'Bearer '+sk,'Content-Type':'application/x-www-form-urlencoded'},body:params.toString()});const data=await res.json();if(data.url)return{statusCode:200,headers,body:JSON.stringify({url:data.url})};return{statusCode:400,headers,body:JSON.stringify({error:data.error?.message||'Fout'})};}if(action==='create_payment_link'){const amt=Math.round(amount)||0;if(!amt)return{statusCode:400,headers,body:JSON.stringify({error:'Bedrag vereist'})};const params=new URLSearchParams({'mode':'payment','payment_method_types[]':'card','payment_method_types[]':'ideal','customer_email':email||'','line_items[0][price_data][currency]':'eur','line_items[0][price_data][product_data][name]':description||('Factuur '+factuurNr),'line_items[0][price_data][unit_amount]':amt,'line_items[0][quantity]':1,'success_url':'https://slimboekhoud.com?factuur_betaald='+encodeURIComponent(factuurNr||''),'cancel_url':'https://slimboekhoud.com'});const res=await fetch('https://api.stripe.com/v1/checkout/sessions',{method:'POST',headers:{'Authorization':'Bearer '+sk,'Content-Type':'application/x-www-form-urlencoded'},body:params.toString()});const data=await res.json();if(data.url)return{statusCode:200,headers,body:JSON.stringify({url:data.url})};return{statusCode:400,headers,body:JSON.stringify({error:data.error?.message||'Fout betaallink'})};}return{statusCode:400,headers,body:JSON.stringify({error:'Onbekende actie'})};}catch(err){return{statusCode:500,headers,body:JSON.stringify({error:err.message})}}};
